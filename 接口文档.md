# AiPPT 后端接口文档（前端对接版）

> 面向前端开发：本文档描述前端需要调用的所有后端接口、请求/响应格式与数据契约。
> 前端**只跟 `main_api`（6800）打交道**，其余服务（personaldb / simpleOutline / slide_agent）由 main_api 内部转发，前端无需关心。

---

## 0. 基本约定

- **Base URL**：开发时前端走 Vite 代理，统一用 `/api` 前缀，代理会把 `/api` 去掉后转发到 `http://127.0.0.1:6800`。
  例：前端 `fetch('/api/templates')` → 后端实际命中 `GET http://127.0.0.1:6800/templates`。
- **返回格式**：正常返回 JSON；错误返回 `{"error": "原因"}` + 对应状态码（400 参数错 / 404 不存在）。
- **CORS**：后端已开 `*`，前端无需额外处理。
- **两种流式协议（务必区分）**：
  | 接口 | Content-Type | 流式格式 |
  |---|---|---|
  | 大纲类 | `text/plain` | 纯文本增量（Markdown 片段），逐段拼接成完整大纲 |
  | 内容类 | `text/event-stream` | SSE，每条 `data: {json}\n\n`，结束发 `data: [DONE]\n\n` |

---

## 1. 接口总览

| 方法 | 路径 | 用途 | 返回 |
|---|---|---|---|
| GET | `/healthz` | 健康检查 | JSON |
| GET | `/templates` | 模板列表 | JSON |
| GET | `/data/{filename}` | 模板封面 SVG / 模板 JSON 文档 | 文件 |
| POST | `/tools/aippt_outline` | 主题/文本 → 大纲 | text/plain 流 |
| POST | `/tools/aippt_outline_from_file` | 上传文件 → 大纲 | text/plain 流 |
| POST | `/tools/aippt` | 大纲 → 逐页 Slide | SSE 流 |
| POST | `/tools/aippt_by_id` | 已上传文件 id → 逐页 Slide | SSE 流 |
| GET | `/files/{user_id}` | 用户已入库文件列表 | JSON |
| GET | `/proxy?url=` | 外链图片代理（跨域） | 图片字节 |

---

## 2. 核心数据契约：Slide Schema

逐页生成接口（`/tools/aippt`、`/tools/aippt_by_id`）每个 `data:` 帧就是一个 `SlideSchema`。
前端类型定义在 `src/types/AIPPT.ts`，**前后端共用这份契约，改接口先改这里**。

```ts
type SlideType = 'cover' | 'contents' | 'transition' | 'content' | 'end' | 'reference'

interface SlideSchema {
  type: SlideType
  data?: SlideData
}

interface SlideData {
  title?: string
  text?: string
  items?: (ContentItem | string)[]   // items 既可以是字符串，也可以是对象
  references?: string[]              // reference 页的参考文献
}

interface ContentItem {
  title?: string
  text?: string
  kind?: 'chart' | 'image'           // 带 kind 表示图表/配图
}

// kind === 'chart'
interface ChartItem extends ContentItem {
  kind: 'chart'
  chartType: 'line' | 'bar' | 'pie' | 'column' | 'ring' | 'area' | 'radar'
  labels: string[]
  series: { name: string; data: number[] }[]
}

// kind === 'image'
interface ImageItem extends ContentItem {
  kind: 'image'
  url?: string
}
```

**各类型 `data` 的实际结构**（后端生成逻辑见 `slide_agent`）：

| type | data 结构 | 示例 |
|---|---|---|
| `cover` | `{ title, text }` | `{"title":"标题","text":"副标题"}` |
| `contents` | `{ items: string[] }` | `{"items":["章节1","章节2"]}` |
| `transition` | `{ title, text }` | `{"title":"章节标题","text":"过渡语"}` |
| `content` | `{ title, items: [{title,text}] }` | `{"title":"页标题","items":[{"title":"要点","text":"正文"}]}` |
| `end` | `{}` | `{}` |
| `reference` | `{ references: string[] }` | `{"references":["ref1","ref2"]}` |

---

## 3. 接口详情

### 3.1 健康检查

```
GET /healthz
```
响应：`{"ok": true, "service": "main_api"}`

### 3.2 模板列表

```
GET /templates
```
响应：
```json
{
  "data": [
    { "name": "科技蓝紫", "id": "template_1", "cover": "/api/data/template_1.svg" },
    { "name": "商务蓝",   "id": "template_2", "cover": "/api/data/template_2.svg" },
    { "name": "活力橙",   "id": "template_3", "cover": "/api/data/template_3.svg" },
    { "name": "清新绿",   "id": "template_4", "cover": "/api/data/template_4.svg" }
  ]
}
```
- `cover` 已经是带 `/api` 前缀的地址，前端可直接 `<img :src="cover">`。

### 3.3 模板封面 / 模板文档

```
GET /data/template_1.svg   → 封面 SVG 图片
GET /data/template_1.json  → 该模板的完整 PPTist 文档（渲染模板用）
```

模板 JSON 文档结构（`width/height/theme/slides`，PPTist 格式）：
```json
{
  "title": "科技蓝紫模板",
  "width": 1000,
  "height": 562.5,
  "theme": { "themeColors": ["#667eea", ...], "fontColor": "#333", "backgroundColor": "#fff" },
  "slides": [
    { "id": "cover-1", "type": "cover", "background": {"type":"solid","color":"#667eea"}, "elements": [...] }
  ],
  "intend": 4
}
```

> 前端「模板选择页」选完模板后，用模板 id 拼 `/data/{id}.json` 拉完整文档来填充渲染。

### 3.4 生成大纲（主题/文本 → 大纲）

```
POST /tools/aippt_outline
Content-Type: application/json

{ "content": "用户输入的主题或长文本", "language": "中文", "model": "deepseek-chat" }
```
- `content` 必填；`language` 默认 `"中文"`；`model` 默认取后端 `.env`。
- 返回 `text/plain` 流，逐段输出 Markdown 大纲，前端按段 `onChunk` 拼接。
- 缺 `content` → 400 `{"error":"content 不能为空"}`。

### 3.5 上传文件生成大纲

```
POST /tools/aippt_outline_from_file
Content-Type: multipart/form-data

file:      <文件二进制>            (必填)
userId:    "1"                     (默认 "1")
fileId:    "xxx"                   (默认 "outline_file"；建议前端生成唯一值，避免覆盖)
language:  "中文"                  (默认 "中文")
```
- 流程：后端先把文件交给 personaldb 解析成 Markdown，再走大纲生成。
- 返回同为 `text/plain` 流。

### 3.6 逐页生成 PPT（大纲 → Slide，核心接口）

```
POST /tools/aippt
Content-Type: application/json

{
  "content": "Markdown 大纲",
  "model": "deepseek-chat",
  "generateFromWebSearch": false,
  "generateFromUploadedFile": false,
  "userId": "1"
}
```
- `content`（Markdown 大纲）必填；其余可选。
- 返回 **SSE** 流，逐页一个 `data:` 帧（每个帧是 `SlideSchema`），结束帧 `data: [DONE]`：

```
data: {"type":"cover","data":{"title":"...","text":"..."}}

data: {"type":"contents","data":{"items":["章节1","章节2"]}}

data: {"type":"transition","data":{"title":"章节1","text":"..."}}

data: {"type":"content","data":{"title":"...","items":[{"title":"要点","text":"正文"}]}}

data: [DONE]
```

前端解析要点（可参考 `src/services/index.ts` 的 `AIPPT_Content` / `AIPPT_StreamEvents`）：
- 逐行读，只处理 `data:` 开头的行；`[DONE]` 表示结束。
- 每页 JSON 解析成 `SlideSchema` 后回调 `onSlide`。

### 3.7 按文件 id 生成 PPT（走知识库检索）

```
POST /tools/aippt_by_id
Content-Type: application/json

{ "fileId": "xxx", "userId": "1", "model": "deepseek-chat" }
```
- `fileId` 必填，对应「上传文件生成大纲」时传入的 fileId。
- 返回格式与 `/tools/aippt` 完全一致（SSE 流）。
- 文件不存在 → 404 `{"error":"文件不存在或未入库"}`；`fileId` 为空 → 400。

### 3.8 用户文件列表

```
GET /files/{user_id}     例：GET /files/1
```
响应（透传 personaldb）：
```json
{ "files": [ { "file_id": "...", "file_name": "...", "file_type": "..." } ] }
```

### 3.9 外链图片代理

```
GET /proxy?url=<外链图片地址>
```
- 返回图片字节（`Content-Type` 取自源站，默认 `image/jpeg`）。
- 用途：前端渲染外部图片时绕开浏览器跨域。缺 `url` → 400。

---

## 4. 内部服务接口（了解即可，前端不直接调用）

这些是 main_api 内部转发的目标，前端**不要直接请求**。列出来便于排查问题。

**simpleOutline（10001）**
- `GET /healthz`
- `POST /generate`  `{ content, language, model }` → text/plain 流

**slide_agent（10011）**
- `GET /healthz`
- `POST /generate`  `{ content, model, generateFromUploadedFile?, userId? }` → SSE 流（`data: {slide}\n\n`，末尾 `data: [DONE]`）

**personaldb（9100）**
- `GET /healthz`
- `POST /search`  `{ userId, query, topk }` → `{ results: [...] }`
- `POST /vectorize/text`  `{ texts: [...] }` → `{ embeddings: [...] }`
- `POST /upload/`  multipart `userId + fileId + (file | url 二选一)` → 入库结果（含 `markdown_content`）
- `GET /files/{user_id}` → 文件列表
- `GET /file/{user_id}/{file_id}` → 该文件的完整 Markdown

---

## 5. 前端对接现状与待办

`src/services/index.ts` 已封装、可直接用：

| 函数 | 对应接口 | 状态 |
|---|---|---|
| `getTemplates()` | `GET /templates` | ✅ 可用 |
| `AIPPT_Outline()` | `POST /tools/aippt_outline` | ✅ 可用 |
| `AIPPT_Content()` | `POST /tools/aippt` | ✅ 可用 |
| `AIPPT_StreamEvents()` | `POST /tools/aippt`（含 status 帧解析） | ✅ 可用 |
| `AIPPT_Outline_From_File()` | `POST /tools/aippt_outline_from_file` | ✅ 可用 |
| `AIPPTByID()` | `POST /tools/aippt_by_id` | ❌ **未实现**（`throw` 占位） |

**待办建议（给组员）**：
1. `AIPPTByID` 尚未实现——参考 `AIPPT_Content` 的 SSE 解析补上（调 `/tools/aippt_by_id`）。
2. 各 view（Input / Outline / TemplateSelect / Generate / Editor / Screen / Settings）目前主要是占位 UI，需按上面的接口接上真实数据流。
3. 模板选择页选完后要 `fetch('/data/{id}.json')` 拉模板文档用于渲染。
